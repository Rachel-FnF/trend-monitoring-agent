# Secrets
.env
.otp
gcp_sa.json
*service*account*.json

# Python
venv/
__pycache__/
*.pyc

# Browser session (device-registered) — do not commit
profile/
state.json
state_chrome.json

# Generated raw data (regenerated daily)
src/data/
src/data.zip

# Shared SQLite DBs (캐릿 유료 분석 결과 — DM으로만 주고받고 git push 금지)
*.db

# PC별 이름표 (set-owner로 저장, 머신마다 다름)
trenddb_owner.txt

# Daily-generated outputs that may contain careet excerpts (유료 미디어 — push 안 함)
docs/dashboard.html
docs/review/digest_*.md
docs/review/deepread_*.md

# 개인 보관용 (인수인계 제외)
docs/legacy-slack.md
