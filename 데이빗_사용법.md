# 📦 트렌드 DB 합치기 — 데이빗 세팅 & 사용 가이드

레이첼(웹 기반 트렌드)과 데이빗(SNS 기반 트렌드)이 각자 모은 데이터를
**DB 파일 하나(`trends.db`)로 주고받아 합치는** 시스템입니다.

- 같은 글을 둘 다 모아도 **자동으로 중복 제거**되고, 누가 모았는지(`rachel`/`david`)가 같이 남아요.
- 서버·인터넷 불필요. 파일 하나만 주고받으면 끝.
- 매일 아침 레이첼 PC가 **자동으로 슬랙 `#트렌드-db` 채널에 `trends.db`를 올려요.** 데이빗은 받아서 합치기만 하면 됩니다.

---

## 🟦 1단계. 처음 한 번만 세팅 (데이빗 PC)

### ① Python 설치
- https://www.python.org/downloads/ 에서 설치
- 설치 화면에서 **"Add Python to PATH" 체크** ✅ (중요!)
- 추가 설치(pip 등) 필요 없음 — SQLite는 파이썬에 기본 포함

### ② 도구 파일 받기
레이첼이 보내주는 **`trenddb.py`** 파일 1개를 아무 폴더에 둠
- 예: `C:\트렌드\trenddb.py`

### ③ PowerShell 열고 그 폴더로 이동
```powershell
cd C:\트렌드
```

### ④ 내 이름표를 'david'로 설정 (딱 1회)
```powershell
python trenddb.py set-owner david
```
> 이러면 앞으로 내가 만드는 데이터엔 자동으로 `david` 이름이 붙어요.

---

## 🟩 2단계. 레이첼 DB 받아서 합치기 (매일 / 핵심)

### ① 슬랙에서 파일 다운로드
- 슬랙 **`#트렌드-db`** 채널에 매일 아침 올라오는 **`trends_2026-XX-XX.db`** 파일을 다운로드
- 받은 파일을 `C:\트렌드\` 폴더(trenddb.py 있는 곳)에 둠

### ② 합치기
```powershell
python trenddb.py merge trends_2026-XX-XX.db
```
(파일 이름은 실제 받은 파일명으로)

### ③ 확인
```powershell
python trenddb.py stats
```
→ `rachel: OOO건 / david: OOO건 / 함께 모은 글: OO건` 이렇게 나오면 성공!

> 💡 `merge`는 중복을 article_id로 자동 제거하고, 레이첼 글엔 `rachel` 이름표를 그대로 보존해요.
> 여러 날 받아서 합쳐도 **계속 누적**됩니다 (덮어쓰기 아님).

---

## 🟨 3단계. 내(데이빗) 데이터를 레이첼에게 보내기

### ① 내 분석결과로 DB 만들기
```powershell
python trenddb.py export --from "C:\내가\SNS분석결과JSON\폴더경로"
```
→ `C:\트렌드\trends.db` 생성 (내 `david` 이름표로)

### ② 레이첼에게 전달
- 만들어진 `trends.db`를 **슬랙 `#트렌드-db` 채널에 드래그&드롭** 하거나 레이첼에게 DM
- 레이첼이 받아서 `merge` 하면 양쪽 데이터가 합쳐짐

> ⏳ **참고:** 데이빗은 SNS(인스타·X·틱톡), 레이첼은 웹 기반이라 데이터 칸 구성이 달라요.
> 데이빗 전용 칸(조회수·좋아요·플랫폼 등)을 어떻게 담을지는 **미팅에서 공통 양식을 정한 뒤**
> 레이첼이 `trenddb.py`를 맞춰줄 예정입니다. 그 전엔 2단계(받아서 합치기)만 테스트해도 충분해요.

---

## 👀 (선택) DB를 표로 눈으로 보기

무료 GUI **DB Browser for SQLite** 추천:
1. https://sqlitebrowser.org/ 설치 (또는 `winget install DBBrowserForSQLite.DBBrowserForSQLite`)
2. 실행 → `trends.db` 열기 → 상단 **`데이터 탐색(Browse Data)`** 탭
3. 엑셀처럼 표로 보임. ⚠️ DB Browser로 파일 열어둔 채로 `merge`하면 "database is locked" 나니, 합칠 땐 닫고 하세요.

---

## 📋 DB 칸 설명 (참고)

| 칸 | 뜻 |
|---|---|
| `article_id` | 글 고유번호 (중복 합치기 기준) |
| `title` `url` | 제목 · 링크 |
| `one_line_summary` `full_description` | 요약 · 상세 |
| `content_category` `topics` `brands_products` | 분류 · 토픽 · 브랜드 |
| `marketing_insight` `target_audience` | 마케팅 시사점 · 타깃 |
| **`date`** | 원문 발행일 |
| **`first_seen`** | 최초 포착일 |
| **`db_added`** | DB 최초 등록일 (일자별 누적 기준) |
| **`updated_at`** | 분석 갱신일 |
| **`owners`** | 누가 모았나 (rachel / david / 둘 다) |

---

## ⚠️ 주의

- **내부용으로만.** 레이첼 데이터에 유료 미디어(캐릿) 분석이 들어있어요 → 공개 채널·외부 메일 금지, 1:1/비공개 채널 공유만.
- 명령 헷갈리면: `python trenddb.py -h` (도움말), `python trenddb.py stats` (현황).

---

## 빠른 요약 (치트시트)

```powershell
# 처음 1회
python trenddb.py set-owner david

# 매일: 레이첼 것 받아서 합치기
python trenddb.py merge 받은파일.db
python trenddb.py stats

# 내 것 보내기 (미팅 후 양식 확정 예정)
python trenddb.py export --from "C:\내SNS분석폴더"
# → 생긴 trends.db 를 #트렌드-db 채널에 올리기
```
